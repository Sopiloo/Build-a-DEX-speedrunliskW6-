{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "build": {
    "env": {}
  },
  "installCommand": "yarn install --ignore-optional --frozen-lockfile",
  "buildCommand": "yarn build:vercel",
  "devCommand": "yarn dev",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["cdg1"]
}
